
spring:
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
      show-sql: true
      generate-ddl: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/user-login-service
    username: sa
    password: